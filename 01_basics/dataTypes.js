"use strict"; // treat all JS code as a newer version of JS

// alert(3 + 3) // we are using the node js not the browser

let name = "yash" //string
let age = 18 // number, bigint
let isLoggedIn = true //boolean
let state; 

// string, number, bigint, boolean, symbol(unique character), null (standalone value), undefined (it means the value is not assigned yet), object

console.log(typeof name);
console.log(typeof age);
console.log(typeof isLoggedIn);
console.log(typeof state);
console.log(typeof null); 
console.log(typeof undefined); 
console.log(typeof Object);
