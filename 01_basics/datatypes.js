"use strict"; // treat all JS code as a newer version

// alert( 3 + 3 ) // Not allowed because we are in nodejs not in browser

let name = "yash" // string
let age = 10 // number, bigint
let isLoggedIn = true // boolean
let city = null; // null is a standalone value
let country; // undefined (means not assigned any value yet)

// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object

